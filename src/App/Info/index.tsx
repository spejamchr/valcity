import { styled } from '../../stitches.config';

const Info = styled('div', {
  position: 'absolute',
  top: 0,
  right: 0,
  backgroundColor: '$gray400',
  margin: 10,
  paddingX: 30,
  paddingY: 10,
});

export default Info;
